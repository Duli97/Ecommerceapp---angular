import { TheDShopValidators } from './the-D-shop-validators';

describe('Luv2ShopValidators', () => {
  it('should create an instance', () => {
    expect(new TheDShopValidators()).toBeTruthy();
  });
});
